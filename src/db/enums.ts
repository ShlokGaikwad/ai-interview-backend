export enum InterviewStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  COMPLETED = 'completed',
}

export enum Speaker {
  AI = 'ai',
  CANDIDATE = 'candidate',
}

export enum UserRole {
  ADMIN = 'admin',
}
